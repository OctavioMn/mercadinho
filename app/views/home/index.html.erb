<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg bg-success bg-gradient ">
        <div class="container-fluid" >
          <p style="color: green"><%= notice %></p>
          <a class= "navbar-brand" href="#">Ruby Market</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
          </button>
            <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
                <ul class="navbar-nav">
                  <li class="nav-item">
                    <a href="clientes#index"  class="nav-link active" aria-current="page" href="#">Cliente</a>
                  </li>
                  <li class="nav-item">
                    <a href="vendas#index" class="nav-link active" href="#">Vendas</a>
                  </li>
                  <li class="nav-item">
                    <a href="produtos#index" class="nav-link active" href="#">Produtos</a>
                  </li>
                  <li class="nav-item text-right">
                    <% if user_signed_in? %>
                        <p> Bem Vindo ao RubyMarket, <%= current_user.email %> </p>
                        <%= link_to "Sair", destroy_user_session_path, method: :DELETE %>
                    <% else %>
                        <%= link_to "Logar", new_user_session_path %>
                    <%end%>
                  </li>
                </ul>
            </div>
        </div>
      </nav>
    </header>

    <main>
      <section>
          <div class="container bg-overlay img-fluid">
            <br><br><br><br><br><br>
            <div class="row text-center">
              <h1>This is a beautiful background image<br> with a transparent overlay.</h1>
                <h4>You can just use the "<strong>.bg-overlay</strong>" class on any container/element,<br>
                  and specify the image you want to use and its height.</h4>
              <br><br>
            </div>
          </div>
      </section>
    </main>

    <footer class="fluid">
      <div class="bg-success bg-gradient"style="text-align:center">
      <br>
        <a> Todos os direitos reservados a <a target="_blank" class="text-dark" href="https://www.linkedin.com/in/josé-octávio-84947b22b/">José Octávio</a> e <a target="_blank" class="text-dark" href="https://www.linkedin.com/in/juanpb1/">Juan pablo</a>
      <br><br>
      </div>
    </footer>

  </body>
</html>
